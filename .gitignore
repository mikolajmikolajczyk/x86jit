# Rust / Cargo
/target
**/*.rs.bk

# Nix
result
result-*

# direnv cache (Nix flake users)
.direnv/

# Tool-managed skills (repoctx etc.) — regenerated locally, not committed.
.claude/skills/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# Local-only
*.local
.envrc.local

# Large, moving-target OCI fixtures — regenerated locally, see fixtures/README.md
x86jit-cli/fixtures/ubuntu*.tar

# repoctx local index (generated)
.repoctx/
x86jit-cli/fixtures/caddy*.tar

# local go-caddy build scratch (task-153/161)
scratch-go/
x86jit-tests/programs/caddy.elf
x86jit-tests/programs/regprobe.elf

# OCI blob cache for image tests (actions/cache persists it in CI)
.oci-cache/
fuzz-avx-findings.log
