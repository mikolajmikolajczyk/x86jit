# Rust / Cargo
/target
**/*.rs.bk

# Nix
result
result-*

# direnv cache (Nix flake users)
.direnv/

# Tool-managed skills (repoctx etc.) — regenerated locally, not committed.
.claude/skills/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# Local-only
*.local
.envrc.local

# Large, moving-target OCI fixtures — regenerated locally, see fixtures/README.md
x86jit-cli/fixtures/ubuntu*.tar

# repoctx local index (generated)
.repoctx/
x86jit-cli/fixtures/caddy*.tar

# local go-caddy build scratch (task-153/161)
scratch-go/
x86jit-tests/programs/caddy.elf
x86jit-tests/programs/regprobe.elf

# OCI blob cache for image tests (actions/cache persists it in CI)
.oci-cache/
fuzz-avx-findings.log

# TomHarte / SingleStepTests 8088 corpus — large per-opcode JSON (~3 MB each,
# 325 files). Fetched locally / in CI, never vendored into git. See
# x86jit-tests/vendor/8088/fetch.sh and x86jit-tests/tests/harte8088.rs.
# The fetch script itself IS tracked; only the downloaded data is ignored.
x86jit-tests/vendor/8088/v1/

# SingleStepTests 80286 corpus — MOO binary per-opcode files (~1-1.5 MB each,
# 326 files) plus metadata.json. Fetched locally / in CI, never vendored into git.
# See x86jit-tests/vendor/80286/fetch.sh and x86jit-tests/tests/ss286.rs.
# The fetch script itself IS tracked; only the downloaded data is ignored.
x86jit-tests/vendor/80286/v1_real_mode/
